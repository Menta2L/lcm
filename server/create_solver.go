package server

import (
	"errors"

	"context"
	"github.com/menta2l/lcm"
)

func (s LcmServer) CreateSolver(ctx context.Context, r *lcm.CreateSolverRequest) (*lcm.CreateSolverResponse, error) {
	return nil, errors.New("not yet implemented")
}
